# TODO

- [x] Update `mobile/app/screens/EditProfileScreen.js` to be functional:
  - [x] Fetch existing profile via `getProfile()` on mount
  - [x] Prefill controlled inputs (full name, email, phone, location, bio)
  - [x] Implement Save button to call `updateProfile()` (and `updateUserLocation()` if needed)
  - [x] Add loading state + basic error handling/alerts
  - [x] Ensure navigation back works
- [x] Run mobile lint/test/build checks (if available)
- [ ] Add backend/save handling for uploaded profile photo (requires API support)
